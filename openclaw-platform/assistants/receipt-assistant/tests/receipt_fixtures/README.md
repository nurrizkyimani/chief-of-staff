# Receipt Fixtures

Put golden-set fixtures here for M1 evaluation.

Suggested structure:

- `images/` for JPG/PNG receipts
- `pdf/` for receipt PDFs
- `expected/` for expected `receipt.v1.1` JSON payloads

Example naming:

- `images/alfamart-001.jpg`
- `expected/alfamart-001.json`
