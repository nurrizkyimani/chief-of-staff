---
name: receipt-intake
description: "Route receipt command to shared pipeline"
metadata:
  openclaw:
    emoji: "🧾"
    events: ["message:preprocessed"]
---

# Receipt Intake Hook

Active OpenClaw hook for RFC-001. Entrypoint is `hooks/receipt-intake/handler.ts`.
